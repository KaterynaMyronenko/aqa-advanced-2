function age(age) {
    let isAdult = false
    if (age >= 18) {
        isAdult = true
        console.log('isAdult', isAdult)
    } else {
        console.log('isAdult', isAdult)
    }
}

age(15)
age(25)
